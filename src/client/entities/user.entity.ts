export class User {
    id: number;
    cpf: string;
    name: string;
    email: string;
    password: string;

    // Optional field for works, can be an empty array or undefined
    // works?: Work[];
}
